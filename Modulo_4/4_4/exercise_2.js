// Declarar variáveis

let numA = 4;
let numB = 7;
let maiorN = 0;

// Programa que retorne o maior de dois numeros:

function maiorOuMenor(numA, numB) {
  if(numA > numB) {
    return(maiorN = numA)
  } else {
   return(maiorN = numB)
  }
  
}

console.log(maiorOuMenor(numA, numB))
