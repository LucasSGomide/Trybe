// Declarar variáveis

let numA = -33

// Programa que retorne postive se positivo e negative se negativo

function positiveOrNegative(numA) {
  if (numA >= 0) {
    return("positve")
  } else {
    return("negative")
  }
}

console.log(positiveOrNegative(numA))